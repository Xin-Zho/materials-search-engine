# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# ── 虚拟环境 ─────────────────────────────────────────
.venv/
venv/
env/

# ── 运行时数据（含敏感信息，绝不提交）───────────────
# data/ 包含：Scopus 登录会话 (state.json)、缓存、导出 CSV、调试文件
data/

# ── v2 主题运行产物（runs/ = 检索/QA/审计输出；决策资产只在 topics/<id> 根）──
topics/*/runs/

# ── 环境变量 / 密钥 ─────────────────────────────────
.env
.env.local
*.key

# ── 编辑器 ──────────────────────────────────────────
.idea/
.vscode/
*.swp
*.zip
result_output.txt
*.txt
